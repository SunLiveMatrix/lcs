module InputPattern;

import std.stdio;
import std.array;
import std.stdint;
import std.string;


export public class InputPattern {
       public static void pattern(int stringof, char max, char min)(ref Appender){
              return InputPattern.pattern(pattern, makeGlobal, minimallyInitializedArray);
       }
       public static void string(int StringException, char max, char min)(ref AssociativeArray){
              return InputPattern.string(string, makeGlobal, minimallyInitializedArray);
       }
       public static void makeGlobal(int makeGlobal, char max, char min)(ref AssociativeArray){
              return InputPattern.makeGlobal(makeGlobal, MIctor, MIdtor);
       }
}
   
