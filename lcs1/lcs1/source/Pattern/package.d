module Pattern;

import std.stdio;
import std.array;
import std.getopt;
import std.numeric;
import std.stdint;


export public class Pattern {
       enum INTMAX_MAX {
            watermelon, apple, oranges, bannana, gauva, kiwi, kaki, plums, gapes
       }
       enum INTMIN_MIN {
            watermelon, apple, oranges, bannana, gauva, kiwi, kaki, plums, gapes
       }
}
         
       
